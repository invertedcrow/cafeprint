import {SIDES} from "../consts";

const initialState = {
    items: [],
    selectedElement: null,
    selectedSide: SIDES.FRONT
};

export const state = { ...initialState };

const getters = {};

const actions = {};

const mutations = {};

export default {
    state,
    getters,
    actions,
    mutations
};